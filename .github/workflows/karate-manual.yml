name: 🚀 Karate API Tests - Manual

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag de pruebas a ejecutar (ej: @login, @security)"
        required: true
        type: string
      ambiente:
        description: "Ambiente a ejecutar (DEV o QA)"
        required: true
        type: choice
        options:
          - DEV
          - QA
        default: QA
      correo:
        description: "Correo donde llegarán las notificaciones"
        required: true
        type: string

jobs:
  run-karate-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:

      # ---------- 1. Checkout ----------
      - name: 📥 Checkout del repositorio
        uses: actions/checkout@v4

      # ---------- 2. JDK ----------
      - name: ☕ Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      # ---------- 3. Notificación antes ----------
      - name: ✉️ Notificar INICIO de ejecución
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.MAIL_SERVER }}
          server_port: ${{ secrets.MAIL_PORT }}
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          to: ${{ github.event.inputs.correo }}
          from: ${{ secrets.MAIL_USERNAME }}
          subject: "🔵 Inicio de ejecución Karate – Tag: ${{ github.event.inputs.tag }}"
          body: |
            Hola,

            Se iniciará la ejecución de pruebas Karate.
            • Tag: ${{ github.event.inputs.tag }}
            • Ambiente: ${{ github.event.inputs.ambiente }}
            • Fecha/Hora: $(date)

            Te notificaré nuevamente al finalizar las pruebas.

      # ---------- 4. Instalar dependencias ----------
      - name: 📦 Instalar dependencias
        run: mvn clean install -DskipTests

      # ---------- 5. Ejecutar pruebas ----------
      - name: 🧪 Ejecutar pruebas Karate con tag
        id: karate
        run: |
          echo "karate_failed=false" >> $GITHUB_OUTPUT
          mvn test -Dkarate.options="--tags ${{ github.event.inputs.tag }}" -Dkarate.env=${{ github.event.inputs.ambiente }} || echo "karate_failed=true" >> $GITHUB_OUTPUT
        continue-on-error: true

      # 6. Crear ZIP único de TODOS los reportes
      - name: 📦 Comprimir SOLO reportes en ZIP
        run: |
            zip -r karate-report.zip target/karate-reports/


      # 7. Subir ZIP como artefacto
      - name: 📤 Subir reporte ZIP como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: karate-report
          path: karate-report.zip
          retention-days: 14

      # ---------- 9. Notificación final ----------
      - name: ✉️ Notificar FIN de ejecución + Adjuntar reporte
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.MAIL_SERVER }}
          server_port: ${{ secrets.MAIL_PORT }}
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          to: ${{ github.event.inputs.correo }}
          from: ${{ secrets.MAIL_USERNAME }}
          subject: "🟢 Resultados Karate – Tag: ${{ github.event.inputs.tag }}"
          html_body: |
            <h2>Resultados de pruebas Karate</h2>
            <p><b>Tag:</b> ${{ github.event.inputs.tag }}</p>
            <p><b>Ambiente:</b> ${{ github.event.inputs.ambiente }}</p>
            <p><b>Estado:</b> <strong>
              ${{ steps.karate.outputs.karate_failed == 'true' && 'FAILED ❌' || 'PASSED ✅' }}
            </strong></p>
            <p><b>Fecha/Hora:</b> $(date)</p>
            <p>Se adjunta el reporte detallado de pruebas.</p>
          attachments: karate-report.zip